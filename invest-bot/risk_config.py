"""
Константы риск-менеджмента — перенесены из config.html (раздел "Риск-менеджмент").
Только то, что реально использует risk.py. Остальные разделы config.html
(AI-модели, новости, дашборд) сюда НЕ переносились — в invest-bot этого
никогда не было, и сейчас не запрашивалось.
"""

# ── Риск от уверенности в сигнале ──────────────────────────────────────────
#   confidence -> risk_pct  stop_width  target
#   55%           0.5%       узкий       1R
#   70%           1.0%       средний     2R
#   85%           1.5%       широкий     3-5R
RISK_MIN_PCT = 0.5   # пол — самый слабый допустимый сигнал (55%)
RISK_MID_PCT = 1.0   # средний сигнал (70%)
RISK_MAX_PCT = 1.5   # сильный сигнал (85%+)

CONF_LOW_THR = 0.55   # ниже — сделка запрещена
CONF_MID_THR = 0.70   # 55-70% -> RISK_MIN_PCT
CONF_HIGH_THR = 0.85  # 70-85% -> RISK_MID_PCT; 85%+ -> RISK_MAX_PCT

# ── Корреляционный риск ──────────────────────────────────────────────────
# Ключ группы -> список человеческих тикеров (strategy.settings.ticker),
# не FIGI. Заполни своими тикерами из settings.ini по мере добавления стратегий.
# Разбито по секторам (а не одной общей группой), чтобы MAX_OPEN_POSITIONS
# мог реально работать — две позиции в разных секторах не считаются
# одной и той же ставкой на рынок, в отличие от пары тикеров одного сектора.
CORR_GROUPS: dict[str, list[str]] = {
    "RU_BANKS": [
        "SBER", "SBRF",  # Сбербанк
        "VTBR",
    ],
    "RU_OIL_GAS": [
        "GAZP", "GAZRF",  # Газпром
        "LKOH", "LKOHF",  # Лукойл
        "ROSN", "ROSNF",  # Роснефть
        "NVTK",
    ],
    "RU_METALS": [
        "GMKN", "GMKNF",  # Норникель
        "RUAL",
    ],
    "RU_DEV": [
        "SMLT",  # Самолёт
    ],
    "RU_TECH": [
        "YDEX", "VKCO",  # Яндекс, VK — рекламный рынок РФ, двигаются синхронно
    ],
    "RU_RETAIL": [
        "MGNT",  # Магнит
    ],
}

# ── Портфельный риск-лимит ───────────────────────────────────────────────
PORTFOLIO_RISK_MAX_PCT = 3.0   # макс. суммарный риск портфеля, % депо (один счёт)
PORTFOLIO_STOP_SQUEEZE = True  # сжимать стоп нового входа при перегрузке

# ── Мульти-счётный портфельный менеджер (PortfolioRiskManager) ───────────
# Лимиты считаются по СУММЕ всех счетов. При достижении дневного лимита
# новые входы блокируются на всех счетах, а giveback ужесточается с
# TRAIL_GIVEBACK_PCT до MULTIPORT_TIGHTENED_GIVEBACK_PCT — иначе открытые
# позиции продолжают давать убыток далеко за лимит.
MULTIPORT_DAILY_LOSS_PCT   = 2.0   # % от суммарного депо
MULTIPORT_WEEKLY_LOSS_PCT  = 5.0
MULTIPORT_MONTHLY_LOSS_PCT = 10.0
MULTIPORT_TOTAL_RISK_MAX_PCT = 3.0  # суммарный открытый риск % всех счетов
# При достижении дневного лимита: входы блокируются, открытые позиции не трогаются.

# ── Прочие параметры ─────────────────────────────────────────────────────
MAX_OPEN_POSITIONS = 2
DAILY_MAX_LOSS_PCT   = 2.0   # макс. дневной убыток, % депо (дефолт; перебивается с дашборда)
WEEKLY_MAX_LOSS_PCT  = 5.0   # макс. недельный убыток, % депо
MONTHLY_MAX_LOSS_PCT = 10.0  # макс. месячный убыток, % депо
BREAKEVEN_AT_R = 1.0
# Скользящий безубыток: стоп подтягивается в три ступени ДО передачи
# управления Chandelier+giveback. До 0.5R — только фиксированный стоп.
BREAKEVEN_SLIDE_START_R = 0.5    # стоп → entry при 0.5R
BREAKEVEN_SLIDE_STEP2_R = 0.75   # стоп → entry + BREAKEVEN_SLIDE_LOCK2_R при 0.75R
BREAKEVEN_SLIDE_LOCK2_R = 0.25   # сколько R защищаем на второй ступени
TRAIL_GIVEBACK_PCT = 30
CHANDELIER_MULT = 3.0

# ── Частичная фиксация по первому тейку ──────────────────────────────────
# При достижении цены take_target закрывается PARTIAL_TP_CLOSE_FRACTION
# позиции, остаток держится с фиксированным уровнем защиты: откат на
# PARTIAL_TP_RETRACE_FRACTION пройденного расстояния "вход -> тейк" от
# самого тейка — закрывает остаток. Включается с дашборда (по умолчанию
# выключено, не меняет текущее поведение без явного решения).
PARTIAL_TP_CLOSE_FRACTION = 0.5
PARTIAL_TP_RETRACE_FRACTION = 1.0 / 3.0

# ── Дальнейшее размазывание фиксации после первого тейка ────────────────
# После half_closed=True цена может пройти ещё дальше — на каждый шаг
# (той же дистанции вход->тейк) сверяем, не просел ли сигнальный edge
# (composite, знаковый по направлению позиции) относительно входа: если
# просел ниже SCALE_OUT_EDGE_DECAY * entry_composite — преимущество исчезает,
# фиксируем ещё SCALE_OUT_CLOSE_FRACTION остатка. Иначе просто подтягиваем
# remainder_stop вперёд и едем дальше. Использует тот же дашборд-тоггл
# partial_tp_enabled, что и первая частичная фиксация.
SCALE_OUT_EDGE_DECAY = 0.4
SCALE_OUT_CLOSE_FRACTION = 0.5

# ── Инвалидация гипотезы входа (вероятность + смена режима) ─────────────
# Идея: стоп — это "аварийный выключатель", а не основной механизм
# выхода. Каждый бар оцениваем (a) вероятность дойти до тейка раньше
# стопа по текущим дрифту/волатильности цены (формула первого выхода
# для дрейфующего броуновского движения) и (b) не сломался ли режим
# рынка (BOCD через regime_confidence из regime.py). Если вероятность
# дойти до тейка упала ниже порога ИЛИ confidence режима обвалился —
# выходим раньше, не дожидаясь стопа. PROB_EXIT_GRACE_R защищает уже
# прибыльные сделки (дальше BREAKEVEN_AT_R эти проверки не душат позицию
# раньше времени — там работает Chandelier/giveback).
PROB_EXIT_ENABLED = True
PROB_EXIT_MIN_PTAKE = 0.12   # ниже этой P(тейк раньше стопа) — выходим
PROB_EXIT_GRACE_R = 0.3      # проверки не выходят, если pnl >= этой доли риска (R)
BOCD_EXIT_CONFIDENCE = 0.55  # regime_confidence ниже — считаем гипотезу сломанной

# ── Поведенческий выход: 2 из 3 (стакан + структура + нитки) ────────────
# Быстрый выход по поведенческим признакам; без grace-окна, т.к. нитки +
# тонкий стакан — это картина перед гэпом (MM убрал ликвидность, движения
# нет, любой крупный ордер даст резкий гэп). Все три сигнала — vote, а не
# самостоятельные правила: требуется ≥ BEHAVIORAL_EXIT_VOTES_NEEDED совпадений.
#
# Если данные свечей не переданы в check_exit — fallback на одиночный
# порог стакана ORDERBOOK_EXIT_THR (обратная совместимость).
ORDERBOOK_EXIT_ENABLED = True
ORDERBOOK_EXIT_THR = -0.5        # fallback: мгновенный выход при экстремальном дисбалансе
BEHAVIORAL_EXIT_VOTES_NEEDED = 2
BEHAVIORAL_EXIT_ORDER_FLOW_THR = -0.3   # мягче -0.5, потому что это один из голосов
BEHAVIORAL_EXIT_HH_BARS = 3             # N баров без нового HH/LL = структура сломана
BEHAVIORAL_EXIT_MOMENTUM_BARS = 5       # окно для расчёта тело/диапазон
BEHAVIORAL_EXIT_MOMENTUM_THR = 0.15     # avg_body/avg_range ниже → нитки
